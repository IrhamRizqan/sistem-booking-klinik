# Project Rules

Do NOT install React.

Do NOT install Vue.

Do NOT install Tailwind CSS.

Do NOT install Next.js.

Do NOT install TypeScript.

Do NOT replace Bootstrap.

Do NOT use Socket.IO.

Do NOT create unnecessary abstractions.

Do NOT over-engineer the project.

Keep everything understandable for university presentation.

Always prefer simplicity.